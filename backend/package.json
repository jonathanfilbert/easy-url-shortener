{
  "private": true,
  "name": "backend",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Jonathan Filbert <jonathan.lisyanto@bpkpenabur.sch.id>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
